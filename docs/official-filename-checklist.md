# Official Test-Plan Filename Checklist

Required PDF rule: `{StudentID}_{ScenarioType}_{YYYYMMDD}`

- Load filename: `performance/scenarios/official/23127027_Load_20260901.js`
- Stress filename: `performance/scenarios/official/23127027_Stress_20260901.js`
- Spike filename: `performance/scenarios/official/23127027_Spike_20260901.js`

Validation regex for k6 plan files: `^23127027_(Load|Stress|Spike)_[0-9]{8}\.js$`

Validation rules:

- Student ID is exactly `23127027`.
- Scenario type is exactly one of `Load`, `Stress`, or `Spike`, with matching case.
- `YYYYMMDD` is eight digits and is a valid calendar date; `.js` is the k6
  source extension and is not part of the PDF's naming stem.
- The date is the actual appropriate human-selected date; it is not predicted, backdated, or generated by AI.
- Each of the three scenario types appears exactly once.

## Human filename action and validation

- Created by: **Phạm Ngọc Gia Bảo — HUMAN student**
- Human-selected date: `20260901`
- Filename validation: **PASS** for all three basenames
- H-010: **DONE BY HUMAN** for the filename action only

The files were created under `performance/scenarios/official/`. Their basenames
match the PDF naming rule, expected student ID, scenario spelling/case, date,
and `.js` tool extension exactly.

## Separate executable-content gate — BLOCKED

The three files are byte-for-byte identical to the reviewed internal wrappers,
but their deeper directory changes the meaning of every relative path. For
example, `../config/workloads.js` resolves from the official directory to the
nonexistent `performance/scenarios/config/workloads.js`, not the reviewed
`performance/config/workloads.js`. Pinned k6 `inspect` failed identically for
Load, Stress, and Spike before HTTP.

If the human keeps the `official/` directory, the semantic path correction for
each wrapper is:

```diff
- import ... from '../config/...';
- import ... from '../lib/...';
- loadPublicCsv: () => open('../data/workflow.csv'),
+ import ... from '../../config/...';
+ import ... from '../../lib/...';
+ loadPublicCsv: () => open('../../data/workflow.csv'),
```

This is a proposed human correction only. Codex did not modify, rename, stage,
or commit the three official wrappers. Official Load preparation/execution is
blocked until the human corrects the paths and requests revalidation.

# Lecturer / TA Clarifications

This register preserves the earlier AI recommendation and the later human
resolution. No question was sent and no lecturer/TA response exists.

## H-002 — k6 raw and HTML report equivalence

| Field | Value |
|---|---|
| Question ID | H-002 |
| Status | **RESOLVED BY HUMAN DECISION** |
| Date asked | **TODO — not sent/confirmed by the human** |
| Response | **TODO — no lecturer/TA response provided** |
| Related requirements | Assignment T1-07 distinct k6-equivalent outputs; T1-14 literal `.jtl` logs and three HTML report folders |
| Technical proposal | Load aggregate/custom summary; Stress native CSV time series; Spike native web-dashboard HTML; authentic native k6 raw data for every scenario |
| Previous AI recommendation | Ask lecturer/TA because the literal `.jtl`/HTML wording appeared ambiguous for k6 |
| Human decision | No external clarification required; use only the official PDF and its explicit rule permitting distinct k6-equivalent outputs |
| Final strategy | Genuine native k6 JSON for every scenario; distinct Load summary, Stress CSV/time-series, and Spike web-dashboard views; never fabricate JMeter artifacts |

### Prepared question

> For the k6 option, may I submit native granular k6 JSON as the raw artifact instead of JMeter `.jtl`, and use three distinct primary k6 outputs—Load end summary, Stress CSV time series, and Spike web-dashboard HTML—as the required listener/report equivalents? If three literal HTML report folders are still mandatory, may I additionally export the native k6 dashboard for each scenario even though those supplemental HTML files share one report type, or which exact k6-compatible formats/tools do you require?

This historical prepared question was **not sent**. It is retained to preserve
the AI audit trail, not as an outstanding action.

### Non-fabrication boundary

- Do not claim the question was sent or answered.
- Do not fill `Date asked` from this file's creation date.
- Do not invent, paraphrase, or infer a lecturer/TA response.
- Do not rename k6 JSON/CSV to `.jtl` or call converted data native JMeter output.
- Preserve the real native k6 raw artifacts regardless of the eventual packaging decision.

# Hardware Screenshot — HUMAN TODO

Status: **NOT CAPTURED**

The human student must capture a real macOS hardware/system screenshot that
shows the required specification evidence and preserves the hostname exactly:

```text
Phams-MacBook-Pro.local
```

Before submission, the human must verify that this hostname is compatible with
previous homework deployment evidence (H-022), save the genuine screenshot,
record its timestamp and SHA-256, and confirm no unrelated sensitive identifier
needs redaction. AI must not generate or mark this screenshot complete.

# Load Screenshot Evidence

The human captured `load-k6-backend-resource.jpg` from the real macOS desktop
during the five-VU Load hold. Codex visually inspected the file and confirmed:

- Activity Monitor shows the `node` backend at PID `42059`;
- visible point-in-time values include 1.4% CPU, 55.6 MB real memory, and 11
  threads;
- the adjacent Codex terminal shows live official Load progress with 5/5 VUs,
  completed iterations, backend-health evidence, and the actual run context.

`load-k6-backend-resource.png` is a real `sips` PNG conversion of that JPEG for
the prescribed filename, not a generated or reconstructed screenshot. The JPEG
is preserved as the human-captured source. These point-in-time visual values are
resource evidence, not a continuous CPU/memory series or capacity claim.

# Spike Screenshot Evidence

`spike-k6-backend-resource.jpg` is the real human-captured source image. It
shows:

- official Spike at 20/20 VUs and 2m59s of 6m05s;
- 154 completed and zero interrupted iterations in the visible live output;
- Activity Monitor backend PID `48405`;
- point-in-time 4.0% CPU, 69.0 MB real memory, and 11 threads.

The adjacent terminal contains Codex's actual live run observation and capture
instruction, including the k6 progress line read from this run's live stdout.
`spike-k6-backend-resource.png` is a genuine `sips` conversion. These are
point-in-time values, not continuous telemetry or a capacity conclusion.

# Stress Screenshot Evidence

`stress-k6-backend-resource.jpg` is the real human-captured source image. It
shows:

- official `wf03_stress` at 20/20 VUs and 8m50s of 12m30s;
- 873 completed and zero interrupted iterations in the visible live output;
- Activity Monitor backend PID `45430`;
- point-in-time 4.0% CPU, 79.9 MB real memory, and 11 threads.

The source JPEG was accidentally saved in the Load screenshot directory and
then moved unchanged to this run. `stress-k6-backend-resource.png` is a genuine
`sips` conversion for the prescribed filename. These are point-in-time visual
resource values, not continuous telemetry or a capacity conclusion.

# Optimization Recommendation Review

Status: **COMPLETED BY HUMAN — NO SUT CHANGE APPLIED**

| Recommendation | Observed symptom / metric evidence | Source/config evidence | Proposed change and expected effect | Risk / applicability | AI suggested classification | Final human verdict | Human explanation |
|---|---|---|---|---|---|---|---|
| Skip redundant successful-login write | Login occurs every iteration; login p95 1.4968–2.1352 ms; no auth failure or proven bottleneck | [`server.js`](../backend/server.js#L35) always runs `UPDATE users SET login_attempts = 0, locked_until = NULL` after valid password | Update only when either field is non-clear; reduce one SQLite write per normal workflow | Must preserve lockout reset semantics and callback error handling; benefit unmeasured | **LIKELY FEASIBLE; INSUFFICIENT BENEFIT EVIDENCE** | **INSUFFICIENT EVIDENCE** | The code does perform a write after every successful login, but login latency was low and no login bottleneck was observed. The optimization may be valid, but the measured benefit is not established by the current tests. |
| Atomic owner/state-scoped cancellation | Cancellation is consistently highest/near-highest p95: 4.45–4.8544 ms; zero failures | [`server.js`](../backend/server.js#L319) performs SELECT then UPDATE by ID | One conditional UPDATE with owner and allowed-state predicate; inspect `changes`; reduce DB round trip and race window | Must exactly preserve 404 vs 400 semantics; behavior regression risk | **LIKELY FEASIBLE WITH SEMANTIC TESTS** | **FEASIBLE WITH SEMANTIC TESTS** | Combining the SELECT and UPDATE may reduce one database round trip and a race window, but it must preserve the current ownership/state rules and 404/400 behavior exactly. It should only be accepted after semantic regression tests. |
| Experiment with WAL and bounded busy timeout | Two writes plus successful-login update per lifecycle; no `SQLITE_BUSY` or p95 degradation through 20 VUs | Read-only PRAGMAs: journal `delete`, busy timeout `0`; one sqlite3 DB handle in [`database.js`](../backend/database.js#L1) | Disposable A/B test WAL plus reviewed timeout; potentially improve read/write concurrency and resilience | No measured contention, durability/operational semantics change, startup reset complicates configuration | **INSUFFICIENT EVIDENCE — EXPERIMENT ONLY** | **INSUFFICIENT EVIDENCE** | The current tests did not observe SQLITE_BUSY, failure growth, or a clear contention knee through the measured loads. WAL/busy-timeout may be worth an experiment, but current evidence does not justify calling it a necessary fix. |
| Add index on `users(email)` | Every iteration logs in by email; login p95 remained ≤2.1352 ms with 22 runtime users | Exact lookup at [`server.js`](../backend/server.js#L35); users table has no index; registration permits duplicate emails | Non-unique index could improve lookup as user count grows | Current table tiny and no lookup degradation; unique index would change behavior/data validity | **INSUFFICIENT EVIDENCE AT CURRENT SCALE** | **INSUFFICIENT EVIDENCE** | Login performs email lookup without an index, but the measured user table was small and login latency remained low. There is no demonstrated lookup degradation at the current tested scale. |
| Add `(user_id, id DESC)` orders index | Order table grew to 1,281 in Stress and 713 in endurance without latency drift | `my-orders` query uses user filter/order at [`server.js`](../backend/server.js#L310), but measured WF-03 probes use primary-key order ID | Could improve a different order-listing workload at larger data volume | Not exercised in measured WF-03 traffic; no supporting symptom | **LIKELY NOT APPLICABLE TO THIS WORKFLOW** | **NOT APPLICABLE TO WF-03** | The measured WF-03 workflow probes orders by primary-key order ID rather than using the user order-list query this index targets. Therefore the proposed index does not address a measured WF-03 symptom. |
| Add normal product-name index | Search p95 1.933–2.2326 ms; five products; no degradation | Search uses leading/trailing wildcard interpolation at [`server.js`](../backend/server.js#L140) | A normal B-tree index is often proposed for search | `LIKE '%term%'` generally cannot use a conventional prefix index; tiny data; parameterization is a separate security/correctness fix | **LIKELY NOT APPLICABLE / POSSIBLE HALLUCINATION IF CLAIMED AS FIX** | **HALLUCINATED / NOT APPLICABLE** | The current search uses a leading-wildcard pattern like LIKE '%term%', for which a normal B-tree prefix index is not a justified performance fix. The dataset is also tiny and no search degradation was observed. |
| Add a database connection pool | No failure knee or saturation observed | SUT uses one local sqlite3 `Database` object, not a client/server DB connection setup | Typical web-performance advice would add a pool | Connection-pool advice does not directly map to this SQLite architecture and has no measured justification | **LIKELY NOT APPLICABLE / POSSIBLE HALLUCINATION** | **HALLUCINATED / NOT APPLICABLE** | The SUT uses a local SQLite database object rather than a client/server database connection model. Generic connection-pool advice does not directly apply to this architecture and is unsupported by the measured evidence. |

Phạm Ngọc Gia Bảo supplied all final verdicts and explanations directly. These
recommendations remain unapplied and no post-optimization result exists.

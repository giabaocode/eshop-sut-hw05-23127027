# HW05 Split Submission Archive

The human-selected grade is `096`. The assignment basename is:

```text
23127027_HW05_AI_Performance_096.zip
```

Because the submission service limits each uploaded file to 20 MB, the genuine
submission ZIP is stored as a standard split ZIP set. The split size is 18 MiB
(18,874,368 bytes), safely below 20,000,000 bytes. Upload **every** file sharing
the basename:

```text
23127027_HW05_AI_Performance_096.z01
23127027_HW05_AI_Performance_096.z02
...
23127027_HW05_AI_Performance_096.zip
```

The `.zip` file is the final segment containing the central directory; it is
not a complete standalone archive without all `.zNN` files. Keep every part in
one directory before extraction. Verify each file against
`23127027_HW05_AI_Performance_096.sha256`.

The archive contains tracked homework/submission artifacts only. It excludes
`.git`, the locally ignored assignment PDF, runtime-private credentials,
disposable clones, and the archive files themselves.

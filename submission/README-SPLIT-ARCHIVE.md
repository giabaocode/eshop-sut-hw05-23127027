# HW05 Split Submission Archive

The human-selected grade is `096`. The assignment basename is:

```text
23127027_HW05_AI_Performance_096.zip
```

The submission service limits each uploaded file to 20 MB. The package is
therefore measured after creation. The current genuine ZIP is below
20,000,000 bytes, so it remains one ordinary ZIP and does **not** require
splitting. Upload this file:

```text
23127027_HW05_AI_Performance_096.zip
```

If later regeneration exceeds 20,000,000 bytes, create a standard split ZIP
using 18 MiB segments and upload every `.zNN` companion plus the final `.zip`
segment. In that case the `.zip` segment is not standalone. Verify the actual
submitted file or files against
`23127027_HW05_AI_Performance_096.sha256`.

The archive contains tracked homework/submission artifacts only. It excludes
`.git`, the locally ignored assignment PDF, runtime-private credentials,
disposable clones, and the archive files themselves.

`git-commit-log.txt` was generated immediately before packaging and therefore
ends at the content-finalization commit included by that snapshot. The later
commit that adds the archive/checksum itself necessarily postdates the archive;
it remains visible in the public student repository without creating a
self-referential package rebuild loop.
